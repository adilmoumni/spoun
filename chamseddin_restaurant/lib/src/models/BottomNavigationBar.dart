abstract class PageBar {
  myAccount(String myAccount);
  payment(String payment);
  changePassword (String changePassword);
  orderListing (String orderListing);
  customerService (String customerService);
  termsAndConditions (String termsAndConditions);
  help (String help);
  diningAreas(var diningArea, PageBar pageBar);
  orderDetails (var orderDetails);
  showQrCode (var qrCode);
  showDiningArea(var diningArea);
  showRestaurant(var restaurant);
  notifications(var notifications);
  dispute(var dispute);
  editCart(var editCart);
  mealCustomization(var meal);
  itemCustomization(var item);
  paymentCart(var payment);
  orderSuccessful(var order);
  changeLanguage(var changeLanguage);
}